---
tags: [auto-learning, longbrain]
date: 2026-05-20
session: d362229a
cwd: C:\Abuss\ai-system-v2
---

# Auto-Learnings — ai-system-v2 — 2026-05-20

> Session: `d362229a` | Generated: 2026-05-20 12:23:28 | Items: 24

---

## Files đã thay đổi
- `Edit` → `C:/Abuss/ai-system-v2/src/index.ts`

## Longbrain tools đã dùng
- `add_learning`: Telegram Bot Polling Conflict — Chat SDK bug + PM2 restart race condition
- `add_learning`: node-telegram-bot-api fix polling conflict — 3 bots 1 process hoạt động OK

---

## [BashSuccess] npm install --production

**Command:** `ssh root@46.250.225.12 "cd /root/ai-system-v2 && npm install --production 2>&1 | tail -5"`
**Result:** found 0 vulnerabilities

---

## [BashSuccess] ssh root@46.250.225.12 "mv /root/ai-system-v2/ecosystem.conf

**Command:** `ssh root@46.250.225.12 "mv /root/ai-system-v2/ecosystem.config.js /root/ai-system-v2/ecosystem.config.cjs && pm2 start /root/ai-system-v2/ecosystem.co`
**Result:** └────┴─────────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 stop sales-agent-v4 && sleep 3 &

**Command:** `ssh root@46.250.225.12 "pm2 stop sales-agent-v4 && sleep 3 && pm2 flush sales-agent-v4 && pm2 start sales-agent-v4 && sleep 8 && pm2 logs sales-agent-`
**Result:** 20|sales-a | Sales Agent v4 started successfully.

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 delete sales-agent-v4 && sleep 5

**Command:** `ssh root@46.250.225.12 "pm2 delete sales-agent-v4 && sleep 5 && pm2 start /root/ai-system-v2/ecosystem.config.cjs && sleep 8 && pm2 logs sales-agent-v`
**Result:** 21|sales-a | Sales Agent v4 started successfully.

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 delete sales-agent-v4 2>/dev/nul

**Command:** `ssh root@46.250.225.12 "pm2 delete sales-agent-v4 2>/dev/null; sleep 35; pm2 start /root/ai-system-v2/ecosystem.config.cjs 2>&1 | tail -3"`
**Result:** └────┴─────────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

---

## [BashSuccess] ssh root@46.250.225.12 " # Stop v4 first to free the polling

**Command:** `ssh root@46.250.225.12 "`
**Result:** [PM2] Applying action stopProcessId on app [sales-agent-v4](ids: [ 22 ])
[PM2] [sales-agent-v4](22) ✓
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬──...

---

## [BashSuccess] ssh root@46.250.225.12 " pm2 stop sales-agent-v4 2>/dev/null

**Command:** `ssh root@46.250.225.12 "`
**Result:** [PM2] Applying action stopProcessId on app [sales-agent-v4](ids: [ 26 ])
[PM2] [sales-agent-v4](26) ✓
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬──...

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 start sales-agent-v4 && sleep 10

**Command:** `ssh root@46.250.225.12 "pm2 start sales-agent-v4 && sleep 10 && pm2 logs sales-agent-v4 --nostream --lines 15 2>&1"`
**Result:** 26|sales-a | Sales Agent v4 started successfully. Polling all bots.

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 start /root/ai-system-v2/dist/in

**Command:** `ssh root@46.250.225.12 "pm2 start /root/ai-system-v2/dist/index.js --name sales-agent-v4 --cwd /root/ai-system-v2 --no-autorestart 2>&1"`
**Result:** └────┴─────────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 restart sales-agent-v4 && sleep 

**Command:** `ssh root@46.250.225.12 "pm2 restart sales-agent-v4 && sleep 20 && pm2 logs sales-agent-v4 --nostream --lines 15 2>&1"`
**Result:** 30|sales-a | Sales Agent v4 started successfully. Polling all bots.

---

## [BashSuccess] ssh root@46.250.225.12 " # Quick manual test: stop bot, manu

**Command:** `ssh root@46.250.225.12 "`
**Result:** Try 5: ok=True desc=ok

---

## [BugFix] Fix: Runtime error

**Problem:** 1→import { z } from 'zod';
     2→import { readFileSync } from 'fs';
     3→import { resolve, dirname } from 'path';
     4→import { fileURLToPath } from 'url';
     5→
     6→import type { BrandConfig, FlowState } from './types.js';
     7→import...
**Solution:** Đã nắm rõ trạng thái. Giờ tôi lên plan.

---

## PLAN — Phase 15 còn lại + VPS

### Tình trạng hiện tại
- VPS **sống lại** (up 5 min), ai-system v1 + admin-panel đang chạy OK
- **ai-system-v2** chưa deploy lên VPS (chỉ có local + tarball `/tmp/ai-system-v2-deploy.tar.gz`)
- LangWatch tests: 3/7 p...

---

## [BugFix] Fix exit code 1: ssh root@46.250.225.12 "pm2

**Problem:** Exit code 1
[PM2] Applying action stopProcessId on app [thuymac-sales](ids: [ 19 ])
[PM2] [thuymac-sales](19) ✓
[PM2] Applying action stopProcessId on app [menhly-sales](ids: [ 9 ])
[PM2] [menhly-sales](9) ✓
[PM2] Applying action stopProcessId on ...
**Fix command:** `ssh root@46.250.225.12 "mv /root/ai-system-v2/ecosystem.config.js /root/ai-system-v2/ecosystem.config.cjs && pm2 start /root/ai-system-v2/ecosystem.co`

---

## [BugFix] Fix error in: ssh root@46.250.225.12 "pm2 restart

**Problem:** │ 19 │ thuymac-sales           │ default     │ 1.0.0   │ fork    │ 0        │ 0      │ 0    │ stopped   │ 0%       │ 0b       │ root     │ disabled │
│ 1  │ webhook-server          │ default     │ 1.0.0   │ fork    │ 702      │ 12m    │ 0    │ onl...
**Fix command:** `ssh root@46.250.225.12 "grep -r 'getUpdates\|polling\|telegram' /root/Abuss/ai-system/core/webhook-server.js 2>/dev/null | head -10"`

---

## [BugFix] Fix exit code 2: ssh root@46.250.225.12 "ls

**Problem:** Exit code 2
**Fix command:** `ssh root@46.250.225.12 "grep -rl 'telegram\|getUpdates' /root/Abuss/ai-system/ --include='*.js' 2>/dev/null | head -10"`

---

## [BugFix] Fix error in: ssh root@46.250.225.12 "pm2 logs

**Problem:** [TAILING] Tailing last 5 lines for [sales-agent-v4] process (change the value with --lines option)
/root/.pm2/logs/sales-agent-v4-error.log last 5 lines:
/root/.pm2/logs/sales-agent-v4-out.log last 5 lines:
20|sales-a | [chat-sdk:telegram] Telegra...
**Solution:** Sales Agent v4 đang chạy clean trên VPS, không có lỗi. Tóm tắt:

---

## Trạng thái hiện tại

| Task | Status |
|------|--------|
| ✅ Build TypeScript | Clean, no errors |
| ✅ Deploy lên VPS | `/root/ai-system-v2/` — npm installed |
| ✅ Fix ESM ecosystem.config | Đổi thành `.cjs` |
| ✅ Fix ADMIN_...

---

## [Decision] Tôi sẽ dùng user's instruction — **deep research t...

**Decision:** Tôi sẽ dùng user's instruction — **deep research trước khi fix tiếp**. Tạm để bot chạy, lưu bài học:

---

## [Learning] - v1 sales agents đang chạy trên cùng Telegram webhook ...

**Insight:** - v1 sales agents đang chạy trên cùng Telegram webhook → cần tắt v1 sales hoặc dùng port riêng cho v2

---

## [Learning] - Tarball cần đúng .env trên VPS (Redis, v98 key, Teleg...

**Insight:** - Tarball cần đúng .env trên VPS (Redis, v98 key, Telegram tokens, admin panel URL)

---

## [Learning] Prompts path dùng `../prompts/` — trên VPS cần copy thư...

**Insight:** Prompts path dùng `../prompts/` — trên VPS cần copy thư mục prompts vào `/root/ai-system-v2/prompts/`.

---

## [Learning] Giờ cập nhật .env trên VPS — cần đảm bảo REDIS_URL poin...

**Insight:** Giờ cập nhật .env trên VPS — cần đảm bảo REDIS_URL point tới localhost và ADMIN_PANEL_URL đúng.
**Context command:** `ssh root@46.250.225.12 "cd /root/ai-system-v2 && npm install --production 2>&1 | tail -5"`

---

## [Learning] v2 dùng **polling mode** — không cần webhook, không xun...

**Insight:** v2 dùng **polling mode** — không cần webhook, không xung đột port với v1.
**Context command:** `ssh root@46.250.225.12 "pm2 stop thuymac-sales menhly-sales thaivangoc-sales 2>&1 && echo '--- v1 sa`

---

## [Config] Environment variables (.env)

**Command:** `cd "C:/Abuss/ai-system-v2" && tar czf /tmp/ai-system-v2-deploy.tar.gz \
  dist/ \
  package.json \
  package-lock.json \
  ecosystem.config.js \
  .en`
**Config:**
```
--- tarball created ---
-rw-r--r-- 1 buitr 197610 87K May 20 18:11 /tmp/ai-system-v2-deploy.tar.gz
```

---

## [Config] SSH config

**Command:** `ssh root@46.250.225.12 "cd /root/ai-system-v2 && npm install --production 2>&1 | tail -5"`
**Config:**
```
69 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities
```

---
> Auto-generated by longbrain-stop-hook v2.0 | [[32 Bai Hoc Duc Ket]]
