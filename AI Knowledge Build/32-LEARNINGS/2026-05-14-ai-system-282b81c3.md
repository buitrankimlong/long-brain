---
tags: [auto-learning, longbrain]
date: 2026-05-14
session: 282b81c3
cwd: C:\Abuss\ai-system
---

# Auto-Learnings — ai-system — 2026-05-14

> Session: `282b81c3` | Generated: 2026-05-14 10:57:56 | Items: 25

---

## Files đã thay đổi
- `Edit` → `C:\Abuss\ai-system\ecosystem.config.js`
- `Write` → `C:\Abuss\ai-system\core\chatwoot-client.js`
- `Edit` → `C:\Abuss\ai-system\core\messenger.js`
- `Edit` → `C:\Abuss\ai-system\brands\common\sales-agent-base.js`
- `Edit` → `C:\Abuss\ai-system\core\webhook-server.js`
- `Edit` → `C:\Abuss\ai-system\config\thuymac.config.js`
- `Edit` → `C:\Abuss\ai-system\config\menhly.config.js`
- `Edit` → `C:\Abuss\ai-system\config\thaivangoc.config.js`
- `Write` → `C:\Abuss\ai-system\scripts\setup-chatwoot-bots.js`
- `Edit` → `C:\Abuss\ai-system\core\lark-client.js`

## Longbrain tools đã dùng
- `add_knowledge`: Chatwoot-Agent-Bot-Setup
- `add_learning`: User preference: Claude chạy mọi lệnh VPS qua SSH, không yêu cầu user tự SSH
- `add_learning`: Chatwoot Cloud KHÔNG bypass được Meta App Review cho Facebook Messenger
- `log_progress`: {"project_name":"Thuy Mac AI System","done":["Dọn dẹp code Chatwoot hoàn toàn (7
- `add_learning`: Bug: session object thiếu channel và msgToken → tools không gửi được ảnh

---

## [BashSuccess] git pull

**Command:** `ssh root@46.250.225.12 "cd /root/Abuss && git pull && pm2 start ecosystem.config.js --only thuymac-bg-watcher 2>&1 | grep -v 'injected\|cron restart'"`
**Result:** └────┴─────────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

---

## [BashSuccess] ssh root@46.250.225.12 "cd /root/Abuss && pm2 restart ecosys

**Command:** `ssh root@46.250.225.12 "cd /root/Abuss && pm2 restart ecosystem.config.js --update-env 2>&1 | grep -E 'reloading|✓|ERROR|WARN' | head -20"`
**Result:** [PM2][WARN] Applications daily-report, weekly-report not running, starting...

---

## [BashSuccess] ssh root@46.250.225.12 "wget -q -O /tmp/install-chatwoot.sh 

**Command:** `ssh root@46.250.225.12 "wget -q -O /tmp/install-chatwoot.sh https://get.chatwoot.app/linux/install.sh && chmod +x /tmp/install-chatwoot.sh && echo 'Sc`
**Result:** Script downloaded OK

---

## [BashSuccess] ssh root@46.250.225.12 "curl -fsSL https://deb.nodesource.co

**Command:** `ssh root@46.250.225.12 "curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | gpg --dearmor -o /usr/share/keyrings/nodesource.gpg && `
**Result:** (Bash completed with no output)

---

## [BashSuccess] git remote

**Command:** `ssh -o StrictHostKeyChecking=no root@46.250.225.12 "cd /root/Abuss && git remote prune origin && git pull 2>&1"`
**Result:** Already up to date.

---

## [BashSuccess] ssh -o StrictHostKeyChecking=no root@46.250.225.12 "echo 'TH

**Command:** `ssh -o StrictHostKeyChecking=no root@46.250.225.12 "echo 'THUYMAC_CHATWOOT_INBOX_ID=109437' >> /root/Abuss/.env && cd /root/Abuss && pm2 restart webho`
**Result:** └────┴─────────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

---

## [BashSuccess] git fetch

**Command:** `ssh root@46.250.225.12 'cd /root/Abuss && git fetch --prune && git reset --hard origin/main && sed -i "/CHATWOOT/d" .env && pm2 restart webhook-server`
**Result:** └────┴─────────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

---

## [BugFix] Fix error in: git add ecosystem.config.js &&

**Problem:** [main c0e08de] fix: bg-watcher cron every 30min (was 2min) to prevent Lark API quota drain
 1 file changed, 1 insertion(+), 1 deletion(-)
To https://github.com/tom-bdm/Abuss.git
   6c2046b..c0e08de  main -> main
**Fix command:** `ssh root@46.250.225.12 "cd /root/Abuss && git pull && pm2 start ecosystem.config.js --only thuymac-bg-watcher 2>&1 | grep -v 'injected\|cron restart'"`

---

## [BugFix] Fix error in: grep -n "channel\|sendFn\|reply\|facebook\|telegra

**Problem:** 5: * - Rate-limited outgoing FB reply queue: 10 msg/sec (FB Messenger limit buffer)
58: * Get or create the outgoing-reply queue for a brand (rate-limited).
108:// FIX: Khi khách gõ 2-3 câu liên tiếp trong vài giây, AI không nên reply lần lượt
183...
**Fix command:** `grep -n "channel\|sendFn\|payload\|sales-agent\|require.*sales\|processIncoming" /c/Abuss/ai-system/core/message-queue.js | head -20`

---

## [BugFix] Fix error in: grep -n "require\|process\|handler\|agent" /c/Abus

**Problem:** 4: * - Per-brand incoming queue: one queue per brandId to isolate processing
11:require('dotenv').config();
12:const Bull = require('bull');
13:const { redis } = require('./redis-client');
15:const REDIS_URL = process.env.REDIS_URL || 'redis://127...
**Fix command:** `grep -n "processMessages\|channel\|sendFn\|payload" /c/Abuss/ai-system/brands/thuymac/sales-agent.js | head -20`

---

## [BugFix] Fix: Runtime error

**Problem:** 1→/**
     2→ * Sales Agent — Thủy Mạc (Painting Gallery)
     3→ * Thin wrapper: config + shared base + message queue consumer.
     4→ * Features: fixed price, no inventory lock.
     5→ */
     6→
     7→require('dotenv').config();
     8→
    ...
**Fix command:** `grep -n "channel\|sendFn\|data\.channel\|data\.platform\|replyFn\|fb\.\|tg\." /c/Abuss/ai-system/brands/common/sales-agent-base.js | head -25`

---

## [BugFix] Fix exit code 128: git add core/chatwoot-client.js

**Problem:** Exit code 128
fatal: not a git repository (or any of the parent directories): .git
**Fix command:** `ssh root@46.250.225.12 "cd /root/Abuss && git pull && echo 'PORT=3001' >> .env && grep '^PORT=' .env | tail -2" 2>&1`

---

## [Decision] 2. Đăng nhập FB → chọn fanpage

**Decision:** 2. Đăng nhập FB → chọn fanpage
**Context:** 1. Vào **Settings → Inboxes → Add Inbox → Facebook** | 3. Xong — khách nhắn vào là bot reply được ngay

---

## [Decision] **Bạn chọn hướng nào?** Nếu muốn Chatwoot Cloud, t...

**Decision:** **Bạn chọn hướng nào?** Nếu muốn Chatwoot Cloud, tôi hướng dẫn từng bước kết nối.

---

## [Decision] - Settings → Inboxes → **New Inbox** → chọn **Face...

**Decision:** - Settings → Inboxes → **New Inbox** → chọn **Facebook**
**Context:** **Bước 2 — Kết nối 3 Facebook Page** | - Đăng nhập FB → chọn Page **Thủy Mạc** → Finish

---

## [Decision] - Đăng nhập FB → chọn Page **Thủy Mạc** → Finish

**Decision:** - Đăng nhập FB → chọn Page **Thủy Mạc** → Finish
**Context:** - Settings → Inboxes → **New Inbox** → chọn **Facebook** | - Làm tương tự cho **Mệnh Lý** và **Thái Vận Ngọc**

---

## [Decision] - Save → gán bot này cho cả 3 inbox (Inbox Setting...

**Decision:** - Save → gán bot này cho cả 3 inbox (Inbox Settings → Configuration → chọn Agent Bot)
**Context:** - Webhook URL: `https://registry-pyramid-cornflake.ngrok-free.dev/webhook/chatwoot`

---

## [Learning] Trước khi code, cần kiểm tra 2 thứ: RAM VPS đủ không (C...

**Insight:** Trước khi code, cần kiểm tra 2 thứ: RAM VPS đủ không (Chatwoot cần ~2GB) và port conflict giữa Chatwoot Rails (port 3000) và app hiện tại:

---

## [Learning] Note: Port 3000 conflict

**Insight:** Port 3000 conflict cần giải quyết.

---

## [Learning] Cần xem webhook-server.js để hiểu flow hiện tại trước k...

**Insight:** Cần xem webhook-server.js để hiểu flow hiện tại trước khi thêm Chatwoot:

---

## [Learning] Cần xem phần cuối của file để thêm đúng chỗ:

**Insight:** Cần xem phần cuối của file để thêm đúng chỗ:

---

## [Learning] Note: Script

**Insight:** Script cần trả lời "no" cho domain/SSL.

---

## [Config] SSH config

**Command:** `ssh root@46.250.225.12 "cd /root/Abuss && git pull && pm2 start ecosystem.config.js --only thuymac-bg-watcher 2>&1 | grep -v 'injected\|cron restart'"`
**Config:**
```
From github.com:tom-bdm/Abuss
   6c2046b..c0e08de  main       -> origin/main
Updating 6c2046b..c0e08de
Fast-forward
 ecosystem.config.js | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
[PM2][WARN] Applications thuymac-bg-watcher not running, starting...
[PM2] App [thuymac-bg-watcher] launched (1 instances)
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬──────...
```

---

## [Config] Environment variables (.env)

**Command:** `ssh root@46.250.225.12 "free -h && echo '---' && grep -E 'PORT|ngrok' /root/Abuss/.env | grep -v '#'" 2>/dev/null`
**Config:**
```
total        used        free      shared  buff/cache   available
Mem:           7.8Gi       374Mi       7.0Gi       1.0Mi       428Mi       7.1Gi
Swap:             0B          0B          0B
---
PORT=3000
WEBHOOK_BASE_URL=https://registry-pyramid-cornflake.ngrok-free.dev
NGROK_STATIC_DOMAIN=registry-pyramid-cornflake.ngrok-free.dev
NGROK_EXE_PATH=/usr/local/bin/ngrok
```

---

## [Config] Config: thuymac.conf

**Command:** `cd /c/Abuss/ai-system && git add core/chatwoot-client.js core/messenger.js core/webhook-server.js brands/common/sales-agent-base.js config/thuymac.con`
**Config:**
```
warning: in the working copy of 'config/menhly.config.js', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'config/thaivangoc.config.js', CRLF will be replaced by LF the next time Git touches it
warning: in the working copy of 'config/thuymac.config.js', CRLF will be replaced by LF the next time Git touches it
[main 1d9bed1] feat: Chatwoot Messenger chan...
```

---
> Auto-generated by longbrain-stop-hook v2.0 | [[32 Bai Hoc Duc Ket]]
