---
tags: [auto-learning, longbrain]
date: 2026-05-20
session: d362229a
cwd: C:\Abuss
---

# Auto-Learnings — Abuss — 2026-05-20

> Session: `d362229a` | Generated: 2026-05-20 13:02:46 | Items: 21

---

## Files đã thay đổi
- `Edit` → `C:/Abuss/ai-system-v2/src/index.ts`
- `Write` → `C:/Abuss/scripts/upload-tranh.js`
- `Write` → `C:/Abuss/scripts/upload-tvn.js`
- `Write` → `C:/Abuss/scripts/create-product-images.js`

## Longbrain tools đã dùng
- `add_learning`: Telegram Bot Polling Conflict — Chat SDK bug + PM2 restart race condition
- `add_learning`: node-telegram-bot-api fix polling conflict — 3 bots 1 process hoạt động OK
- `add_learning`: Sharp composite tạo ảnh sản phẩm — tranh khung gỗ + SIM card template

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 delete sales-agent-v4 2>/dev/nul

**Command:** `ssh root@46.250.225.12 "pm2 delete sales-agent-v4 2>/dev/null; cd /root/ai-system-v2 && tar xzf /tmp/ai-system-v2-deploy.tar.gz && sleep 5 && pm2 star`
**Result:** 24|sales-a | [thuymac] [poll] Error: Conflict: terminated by other getUpdates request; make sure that only one bot instance is running

---

## [BashSuccess] ssh root@46.250.225.12 " pm2 stop sales-agent-v4 2>/dev/null

**Command:** `ssh root@46.250.225.12 "`
**Result:** [PM2] Applying action stopProcessId on app [sales-agent-v4](ids: [ 26 ])
[PM2] [sales-agent-v4](26) ✓
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬──...

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 start sales-agent-v4 && sleep 10

**Command:** `ssh root@46.250.225.12 "pm2 start sales-agent-v4 && sleep 10 && pm2 logs sales-agent-v4 --nostream --lines 15 2>&1"`
**Result:** 26|sales-a | Sales Agent v4 started successfully. Polling all bots.

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 start /root/ai-system-v2/dist/in

**Command:** `ssh root@46.250.225.12 "pm2 start /root/ai-system-v2/dist/index.js --name sales-agent-v4 --cwd /root/ai-system-v2 --no-autorestart 2>&1"`
**Result:** └────┴─────────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 restart sales-agent-v4 && sleep 

**Command:** `ssh root@46.250.225.12 "pm2 restart sales-agent-v4 && sleep 20 && pm2 logs sales-agent-v4 --nostream --lines 15 2>&1"`
**Result:** 30|sales-a | Sales Agent v4 started successfully. Polling all bots.

---

## [BashSuccess] ssh root@46.250.225.12 " # Quick manual test: stop bot, manu

**Command:** `ssh root@46.250.225.12 "`
**Result:** Try 5: ok=True desc=ok

---

## [BashSuccess] npm install --production

**Command:** `ssh root@46.250.225.12 "cd /root/ai-system-v2 && npm install --production 2>&1 | tail -5"`
**Result:** found 0 vulnerabilities

---

## [BugFix] Fix error in: ssh root@46.250.225.12 "pm2 flush

**Problem:** [PM2] Flushing:
[PM2] /root/.pm2/logs/sales-agent-v4-out.log
[PM2] /root/.pm2/logs/sales-agent-v4-error.log
[PM2] Logs flushed
[TAILING] Tailing last 10 lines for [sales-agent-v4] process (change the value with --lines option)
/root/.pm2/logs/sale...
**Fix command:** `ssh root@46.250.225.12 "ps aux | grep 'dist/index\|ai-system-v2' | grep -v grep"`

---

## [BugFix] Fix exit code 1: ssh root@46.250.225.12 "ps

**Problem:** Exit code 1
root         702  0.5  1.0 1528892 83772 ?       Ssl  13:02   0:09 node /root/Abuss/core/webhook-server.js
root        7080  2.6  1.0 1667124 86124 ?       Ssl  13:31   0:01 node /root/ai-system-v2/dist/index.js
---
**Fix command:** `tasklist | grep -i node 2>/dev/null || wmic process where "name='node.exe'" get processid,commandline 2>/dev/null | head -10`

---

## [BugFix] Fix exit code 255: ssh root@46.250.225.12 "

**Problem:** Exit code 255
[PM2] Applying action deleteProcessId on app [sales-agent-v4](ids: [ 28 ])
[PM2] [sales-agent-v4](28) ✓
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──...
**Fix command:** `ssh root@46.250.225.12 "pm2 list --no-color 2>&1 | grep sales-agent"`

---

## [BugFix] Fix exit code 1: ssh root@46.250.225.12 "pm2

**Problem:** Exit code 1
**Fix command:** `ssh root@46.250.225.12 "pm2 list --no-color 2>&1 | tail -25"`

---

## [BugFix] Fix error in: ssh root@46.250.225.12 "sleep 20

**Problem:** [TAILING] Tailing last 15 lines for [sales-agent-v4] process (change the value with --lines option)
/root/.pm2/logs/sales-agent-v4-error.log last 15 lines:
29|sales-a | [thuymac] [poll] Error: Conflict: terminated by other getUpdates request; make...
**Fix command:** `ssh root@46.250.225.12 "ps aux | grep 'dist/index' | grep -v grep && echo '---' && pm2 describe sales-agent-v4 2>&1 | grep -E 'pid|restarts|status'"`

---

## [Decision] Tôi sẽ dùng user's instruction — **deep research t...

**Decision:** Tôi sẽ dùng user's instruction — **deep research trước khi fix tiếp**. Tạm để bot chạy, lưu bài học:

---

## [Decision] Ảnh gốc có **nền trắng xung quanh** bức tranh. Fol...

**Decision:** Ảnh gốc có **nền trắng xung quanh** bức tranh. Folder `cropped` đã crop sát tranh rồi. Tôi sẽ dùng ảnh từ folder `cropped` (đã crop sẵn), không cần xóa background nữa.

---

## [Decision] Tôi sẽ dùng **Option A cho cả hai**:

**Decision:** Tôi sẽ dùng **Option A cho cả hai**:
**Context:** - **Thủy Mạc**: AI tạo background phòng khách có **khung tranh trống** → Sharp paste tranh gốc vào khung → ảnh final 100% giữ nguyên tranh

---

## [Learning] Vấn đề không phải code sai mà là Telegram API behavior.

**Insight:** Vấn đề không phải code sai mà là Telegram API behavior.
**Context command:** `ssh root@46.250.225.12 "
pm2 stop sales-agent-v4 2>/dev/null
sleep 5

# Test ONE bot manually — just`

---

## [Learning] Conflict error không phải fatal — chỉ là noise trong lo...

**Insight:** Conflict error không phải fatal — chỉ là noise trong logs.
**Context command:** `ssh root@46.250.225.12 "pm2 start /root/ai-system-v2/dist/index.js --name sales-agent-v4 --cwd /root`

---

## [Learning] - Chỉ cần thêm **chat widget frontend** trên website + ...

**Insight:** - Chỉ cần thêm **chat widget frontend** trên website + **webhook API** nhận tin từ widget

---

## [Learning] Deploy lên VPS — lần này cần upload cả `node_modules` m...

**Insight:** Deploy lên VPS — lần này cần upload cả `node_modules` mới vì thêm package:

---

## [Config] SSH config

**Command:** `ssh root@46.250.225.12 "pm2 delete sales-agent-v4 2>/dev/null; cd /root/ai-system-v2 && tar xzf /tmp/ai-system-v2-deploy.tar.gz && sleep 5 && pm2 star`
**Config:**
```
[PM2] Applying action deleteProcessId on app [sales-agent-v4](ids: [ 23 ])
[PM2] [sales-agent-v4](23) ✓
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──────────┬──────────┐
│ id │ name                    │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │ cpu      │ mem      │ user     │ watc...
```

---

## [Config] Environment variables (.env)

**Command:** `cd "C:/Abuss/ai-system-v2" && npx tsc 2>&1 && echo "BUILD OK" && tar czf /tmp/ai-system-v2-deploy.tar.gz dist/ package.json package-lock.json ecosyste`
**Config:**
```
BUILD OK
UPLOADED
```

---
> Auto-generated by longbrain-stop-hook v2.0 | [[32 Bai Hoc Duc Ket]]
