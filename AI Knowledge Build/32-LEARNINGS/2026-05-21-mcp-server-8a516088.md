---
tags: [auto-learning, longbrain]
date: 2026-05-21
session: 8a516088
cwd: C:\AI Build Learning\mcp-server
---

# Auto-Learnings — mcp-server — 2026-05-21

> Session: `8a516088` | Generated: 2026-05-21 03:40:15 | Items: 9

---

## Mục tiêu session
- Chung ta bat dau build long-brain v7

## Files đã thay đổi
- `Write` → `C:\AI Build Learning\mcp-server\db.js`
- `Write` → `C:\AI Build Learning\mcp-server\embeddings.js`
- `Write` → `C:\AI Build Learning\mcp-server\indexer.js`
- `Write` → `C:\AI Build Learning\mcp-server\hybrid-search.js`
- `Edit` → `C:\AI Build Learning\mcp-server\server.js`
- `Edit` → `C:\AI Build Learning\mcp-server\package.json`
- `Edit` → `C:\Users\buitr\.claude\projects\C--AI-Build-Learning\memory\MEMORY.md`

## Longbrain tools đã dùng
- `add_learning`: Longbrain v7 Build - Hybrid Search FTS5 + sqlite-vec + RRF

---

## [BugFix] Fix error in: cd "C:\AI Build Learning\mcp-server"

**Problem:** === FULL INTEGRATION TEST ===

✅ DB initialized at longbrain.db

Starting vault reindex...
  Embedding: 0/408
❌ Error: Batch 1/21 failed after 3 retries: Request timeout
Error: Batch 1/21 failed after 3 retries: Request timeout
    at embedTexts (...
**Solution:** V98 API timeout khi embed 408 files cùng lúc. Cần tăng timeout và thêm logic retry tốt hơn. Vấn đề là timeout 30s quá ngắn cho batch 20 texts dài. Sửa:

---

## [Decision] V98 tiếp tục là lựa chọn đúng cho embeddings. Deep...

**Decision:** V98 tiếp tục là lựa chọn đúng cho embeddings. DeepSeek key giữ lại dùng cho AI chat agents.

---

## [Decision] Chọn Claude

**Decision:** **Restart Claude Code** — MCP server sẽ tự reload v7, sau đó mọi search sẽ dùng hybrid search (BM25 + semantic). Lần restart đầu tiên server detect DB đã có sẵn → incremental check → skip 408 files → **khởi động nhanh, 0 tokens**.

---

## [Learning] Now `add_never_again` — need to index both the individu...

**Insight:** Now `add_never_again` — need to index both the individual file and the updated index:

---

## [Learning] Trước tiên cần install dependencies:

**Insight:** Trước tiên cần install dependencies:

---

## [Learning] Test Phase 2 (embeddings) — cần V98 API key:

**Insight:** Test Phase 2 (embeddings) — cần V98 API key:

---

## [Learning] Về câu hỏi DeepSeek API: **Không nên thay V98 bằng Deep...

**Insight:** Về câu hỏi DeepSeek API: **Không nên thay V98 bằng DeepSeek cho embeddings**.

---

## [Learning] DeepSeek API key tốt cho **chat/reasoning tasks**, khôn...

**Insight:** DeepSeek API key tốt cho **chat/reasoning tasks**, không phải embeddings

---

## [Config] Config file

**Command:** `cd "C:\AI Build Learning\mcp-server" && timeout 15 node -e "
const { scanVault, parseFrontmatter } = require('./indexer');
const { classifyQuery } = r`
**Config:**
```
✅ scanVault: 408 files
✅ parseFrontmatter: { tags: '[test]', description: 'Hello' }
✅ classifyQuery("VPS") → KEYWORD 
✅ classifyQuery("cách deploy server lên VPS?") → HYBRID 
✅ classifyQuery("server.js") → KEYWORD 
✅ classifyQuery("triển khai ứng dụng") → HYBRID 
✅ classifyQuery("Docker") → KEYWORD 
✅ classifyQuery("làm sao lưu kiến thức") → HYBRID 

✅ SCANNER + CLASSIFIER TESTS PASSED
```

---
> Auto-generated by longbrain-stop-hook v2.0 | [[32 Bai Hoc Duc Ket]]
