---
tags: [auto-learning, longbrain]
date: 2026-05-21
session: 4e1e10d8
cwd: C:\AI Build Learning\.claude\hooks
---

# Auto-Learnings — hooks — 2026-05-21

> Session: `4e1e10d8` | Generated: 2026-05-21 03:07:18 | Items: 16

---

## Mục tiêu session
- Deep research xem có cách nào tôi chỉnh claude code để khi tôi chỉ cần nhập Claude thì nó sẽ là chế độ bypass permission ko, vì tôi lúc nào cũng dùng chế độ này

## Files đã thay đổi
- `Write` → `C:\Users\buitr\.bashrc`
- `Write` → `C:\AI Build Learning\mcp-server\UPGRADE-PLAN-v7.md`
- `Edit` → `C:\Users\buitr\.claude\projects\C--AI-Build-Learning\memory\MEMORY.md`

## Longbrain tools đã dùng
- `add_learning`: V98 API HỖ TRỢ embeddings endpoint — text-embedding-3-small hoạt động
- `add_knowledge`: Longbrain-v7-Hybrid-Search-Architecture
- `add_learning`: Longbrain v7 Plan hoàn thành — 6 research agents + hands-on testing
- `add_learning`: ECC patterns áp dụng cho Longbrain v7 — token optimization + graceful degradation

---

## [BugFix] Fix error in: cd "C:/AI Build Learning/mcp-server"

**Problem:** === Vietnamese Semantic Similarity Tests ===

SAME MEANING (should be HIGH > 0.7):
  "deploy server VPS" vs "triển khai máy chủ VPS":   0.7394
  "cách fix bug database" vs "Cach fix bug database": 0.8564

RELATED (should be MEDIUM 0.4-0.7):
  "dep...
**Solution:** Kết quả similarity test **hoàn hảo**:

| Test | Cosine | Expected | Status |
|------|--------|----------|--------|
| "deploy server VPS" vs "triển khai máy chủ VPS" | **0.74** | HIGH | ✅ |
| "cách fix bug" vs "Cach fix bug" (no diacritics) | **0.86** | HIGH | ✅ |
| "deploy VPS" vs "PM2 Nginx" (re...

---

## [BugFix] Fix exit code 1: cd "C:/AI Build

**Problem:** Exit code 1
✅ Schema created: documents + docs_fts + docs_vec + triggers
[eval]:73
  insertVec.run(id, Buffer.from(fakeEmbeddings[doc.path].buffer));
            ^

SqliteError: Only integers are allows for primary key values on docs_vec
    ...
**Solution:** Bug: sqlite-vec vec0 primary key phải là integer, nhưng autoincrement bắt đầu từ 1. Sửa:

---

## [BugFix] Fix: Runtime error

**Problem:** 290→---
   291→
   292→## 13. COMMANDS QUAN TRỌNG
   293→
   294→| Command | Mục đích |
   295→|---|---|
   296→| `/plan` | Plan trước khi code → `.claude/plans/*.plan.md` |
   297→| `/code-review` | 8-phase review, severity levels |
   298→| `/le...
**Solution:** Đã có đủ context. Tôi đã hiểu rõ ECC. Giờ phân tích xem có gì từ ECC nên áp dụng cho Longbrain v7:

---

## ECC Patterns Áp Dụng Cho Longbrain v7

Từ repo ECC (https://github.com/affaan-m/ECC), có **3 patterns quan trọng** nên tham khảo:

### 1. MCP Token Overhead — GIẢM tools
ECC ghi rõ: **"Mỗi ...

---

## [BugFix] Fix exit code 1: find ~/.claude/hooks/ -name

**Problem:** Exit code 1
---
basename: extra operand ‘Learning/.claude/hooks/longbrain-autosave.js’
Try 'basename --help' for more information.
===  ===
0
basename: extra operand ‘Learning/.claude/hooks/longbrain-context.js’
Try 'basename --help' for more info...
**Solution:** Bây giờ tôi hiểu rõ rồi. Đây là phân tích:

---

## Tình trạng Skills hiện tại

| Metric | Giá trị |
|--------|---------|
| Skills đã cài | **231** (gần như toàn bộ ECC) |
| Skill Router routes | **27 routes** (trong hook longbrain-context.js) |
| Cách hoạt động | Hook match keywords trong prompt...

---

## [Decision] Câu hỏi rất hay — đây là quyết định kiến trúc quan...

**Decision:** Câu hỏi rất hay — đây là quyết định kiến trúc quan trọng. Để tôi research song song: phân tích hiện trạng vault + deep research RAG/Vector DB.

---

## [Decision] | Component | Lựa chọn | Lý do |

**Decision:** | Component | Lựa chọn | Lý do |
**Context:** |-----------|----------|-------|

---

## [Decision] 1. **V98 có hỗ trợ `/v1/embeddings` không?** (crit...

**Decision:** 1. **V98 có hỗ trợ `/v1/embeddings` không?** (critical — quyết định dùng V98 hay Ollama local)
**Context:** 2. **sqlite-vec hoạt động trên Windows Node.js không?**

---

## [Decision] - [ ] Quyết định tokenizer: `unicode61` vs `porter...

**Decision:** - [ ] Quyết định tokenizer: `unicode61` vs `porter` vs custom
**Context:** - [ ] Test: query "server" có match content chứa "máy chủ" không? (→ biết giới hạn FTS5) | - [ ] `add_learning()` kết quả test FTS5 Vietnamese

---

## [Decision] - [ ] `add_learning()` parser chọn

**Decision:** - [ ] `add_learning()` parser chọn
**Context:** - [ ] Test: parse 5 file MD thật từ vault, verify metadata extraction

---

## [Learning] - Query mơ hồ ("bug database hôm trước") — cần semantic...

**Insight:** - Query mơ hồ ("bug database hôm trước") — cần semantic understanding

---

## [Learning] Tách server.js thành modules — **NÊN (đã trong plan)**

**Insight:** Tách server.js thành modules — **NÊN (đã trong plan)**

---

## [Learning] Sau khi v7 có hybrid search trong DB, hook nên query DB...

**Insight:** Sau khi v7 có hybrid search trong DB, hook nên query DB trực tiếp thay vì scan filesystem.

---

## [Learning] Fix package.json version — **NÊN (trivial)**

**Insight:** Fix package.json version — **NÊN (trivial)**

---

## [Learning] **Chỉ nên thêm 1 việc vào plan v7**: Hook `longbrain-co...

**Insight:** **Chỉ nên thêm 1 việc vào plan v7**: Hook `longbrain-context.js` query từ SQLite DB thay vì scan filesystem.

---

## [Config] Nginx config

**Command:** `cd "C:/AI Build Learning/mcp-server" && node -e "
const Database = require('better-sqlite3');
const sqlite_vec = require('sqlite-vec');

// Test 1: Cr`
**Config:**
```
✅ sqlite-vec loaded successfully
✅ sqlite-vec version: v0.1.9
✅ Vector table created (512 dims)
✅ Inserted 3 vectors
✅ KNN query results:
   doc2 distance: 0.226275
   doc1 distance: 2.036470
   doc3 distance: 7.014494
✅ FTS5 table created with unicode61 remove_diacritics 2
✅ FTS5 search "VPS": [ 'Deploy VPS' ]
✅ FTS5 search "cach" (no diacritics): [ 'Deploy VPS' ]
✅ FTS5 search "triển khai": [...
```

---

## [Config] Config file

**Command:** `cd "C:/AI Build Learning/mcp-server" && node -e "
const https = require('https');
const API_KEY = 'sk-rdysXFstsySd1RqSc33OfP4tCQXqGneOK00e5Ob8G6ACXyk9`
**Config:**
```
Batch 30: ✅ OK (30 results, 360 tokens)
Batch 40: ✅ OK (40 results, 480 tokens)
Batch 50: ✅ OK (50 results, 600 tokens)
Long text (24500 chars): ✅ OK, tokens: 8001
```

---
> Auto-generated by longbrain-stop-hook v2.0 | [[32 Bai Hoc Duc Ket]]
