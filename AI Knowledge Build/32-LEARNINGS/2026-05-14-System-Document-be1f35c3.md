---
tags: [auto-learning, longbrain]
date: 2026-05-14
session: be1f35c3
cwd: C:\System Document
---

# Auto-Learnings — System Document — 2026-05-14

> Session: `be1f35c3` | Generated: 2026-05-14 08:09:45 | Items: 12

---

## Mục tiêu session
- Bat dau du an moi
- Đây là dự án tạo ra 1 tools hỗ trợ tôi lấy token access fanpage facebook instagram và cả thread không hết hạn.

## Files đã thay đổi
- `Write` → `C:\System Document\token-tool\index.js`
- `Write` → `C:\System Document\token-tool\server.js`
- `Write` → `C:\System Document\token-tool\start.bat`

## Longbrain tools đã dùng
- `init_project`: System Document
- `add_learning`: Lay Facebook Page Token vinh vien bang Node.js local server
- `log_progress`: {"project_name":"System Document","done":["Build tool lấy Facebook Page Access T
- `add_knowledge`: Chatwoot-Facebook-Messenger-No-Meta-Review

---

## [BugFix] Fix error in: cd "C:\System Document" &&

**Problem:** Exception calling "OpenRead" with "1" argument(s): "The process cannot access the file 'C:\System Document\Hu?ng d?n 
l?y token kh�ng h?t h?n.docx' because it is being used by another process."
At line:3 char:1
+ $zip = [System.IO.Compression.Z...
**Fix command:** `powershell -Command "
Copy-Item 'C:\System Document\Hướng dẫn lấy token không hết hạn.docx' 'C:\System Document\temp_token.docx' -Force
Add-Type -Asse`

---

## [BugFix] Fix: Runtime error

**Problem:** <tool_use_error>InputValidationError: Write failed due to the following issues:
The required parameter `file_path` is missing
An unexpected parameter `path` was provided

This tool's schema was not sent to the API — it was not in the discovered-to...
**Fix command:** `mkdir -p "C:\System Document\token-tool"`

---

## [Decision] Tôi sẽ dùng **"System Document"** làm tên dự án (t...

**Decision:** Tôi sẽ dùng **"System Document"** làm tên dự án (theo folder hiện tại).

---

## [Decision] Chọn Next.js

**Decision:** 2. **Stack dự kiến** — Công nghệ sẽ dùng? (VD: Next.js + Supabase, Python FastAPI, v.v.)
**Context:** 1. **Mô tả ngắn** — Dự án này làm gì? (VD: quản lý tài liệu nội bộ, hệ thống lưu trữ file, v.v.)

---

## [Decision] 3. **Short-lived Token** — lấy thủ công tại Graph ...

**Decision:** 3. **Short-lived Token** — lấy thủ công tại Graph Explorer (chọn đủ permissions)
**Context:** 2. **App Secret** — App Settings > Basic > click "Show" | 4. **Page ID** — ID của fanpage

---

## [Decision] **Bước 2** — Tick chọn các quyền:

**Decision:** **Bước 2** — Tick chọn các quyền:
**Context:** - `pages_show_list`

---

## [Decision] 2. Đăng nhập Facebook → chọn 4 fanpage

**Decision:** 2. Đăng nhập Facebook → chọn 4 fanpage
**Context:** 1. Vào Chatwoot → **Settings → Inboxes → Add Inbox → Facebook** | 3. Chatwoot tự xử lý webhook với FB_APP_ID/SECRET của bạn → không cần Meta verify

---

## [Learning] ## Tool cần build — tôi cần xác nhận:

**Insight:** ## Tool cần build — tôi cần xác nhận:

---

## [Learning] Trước khi build, tôi cần biết **dạng tool**:

**Insight:** Trước khi build, tôi cần biết **dạng tool**:

---

## [Learning] **C) File HTML tĩnh** — không cần server, mở thẳng bằng...

**Insight:** **C) File HTML tĩnh** — không cần server, mở thẳng bằng trình duyệt:

---

## [Learning] Không cần cài thêm gì ngoài Node.js (đã có v24).

**Insight:** Không cần cài thêm gì ngoài Node.js (đã có v24).

---

## [Learning] Không cần cài thêm gì — Node.js v24 đã đủ.

**Insight:** Không cần cài thêm gì — Node.js v24 đã đủ.

---
> Auto-generated by longbrain-stop-hook v2.0 | [[32 Bai Hoc Duc Ket]]
