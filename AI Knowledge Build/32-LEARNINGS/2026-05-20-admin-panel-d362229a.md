---
tags: [auto-learning, longbrain]
date: 2026-05-20
session: d362229a
cwd: C:\Abuss\admin-panel
---

# Auto-Learnings — admin-panel — 2026-05-20

> Session: `d362229a` | Generated: 2026-05-20 13:32:26 | Items: 21

---

## Files đã thay đổi
- `Edit` → `C:/Abuss/ai-system-v2/src/index.ts`
- `Write` → `C:/Abuss/scripts/upload-tranh.js`
- `Write` → `C:/Abuss/scripts/upload-tvn.js`
- `Write` → `C:/Abuss/scripts/create-product-images.js`
- `Edit` → `C:/Abuss/admin-panel/src/app/(dashboard)/products/[brandId]/page.tsx`

## Longbrain tools đã dùng
- `add_learning`: Telegram Bot Polling Conflict — Chat SDK bug + PM2 restart race condition
- `add_learning`: node-telegram-bot-api fix polling conflict — 3 bots 1 process hoạt động OK
- `add_learning`: Sharp composite tạo ảnh sản phẩm — tranh khung gỗ + SIM card template
- `add_learning`: Admin Panel Marketing Redesign Plan — Phase A-D

---

## [BashSuccess] npm install --production

**Command:** `ssh root@46.250.225.12 "cd /root/ai-system-v2 && npm install --production 2>&1 | tail -5"`
**Result:** found 0 vulnerabilities

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 start /root/ai-system-v2/dist/in

**Command:** `ssh root@46.250.225.12 "pm2 start /root/ai-system-v2/dist/index.js --name sales-agent-v4 --cwd /root/ai-system-v2 --kill-timeout 10000 && sleep 8 && p`
**Result:** 33|sales-a | [thaivangoc] Polling started

---

## [BashSuccess] ssh root@46.250.225.12 "pm2 delete sales-agent-v4 2>/dev/nul

**Command:** `ssh root@46.250.225.12 "pm2 delete sales-agent-v4 2>/dev/null; pm2 start /root/ai-system-v2/dist/index.js --name sales-agent-v4 --cwd /root/ai-system-`
**Result:** 34|sales-a | [menhly] Bot: @menhlybot (8777029279) — polling active

---

## [BashSuccess] scp /tmp/admin-panel-deploy.tar.gz root@46.250.225.12:/tmp/ 

**Command:** `scp /tmp/admin-panel-deploy.tar.gz root@46.250.225.12:/tmp/ 2>&1 && ssh root@46.250.225.12 "pm2 stop admin-panel && cd /root/admin-panel && tar xzf /t`
**Result:** 18|admin-p |  ✓ Ready in 607ms

---

## [BugFix] Fix error in: ssh root@46.250.225.12 "sleep 15

**Problem:** 4
TG connections
[TAILING] Tailing last 15 lines for [sales-agent-v4] process (change the value with --lines option)
/root/.pm2/logs/sales-agent-v4-error.log last 15 lines:
/root/.pm2/logs/sales-agent-v4-out.log last 15 lines:
32|sales-a | Waiting...
**Fix command:** `ssh root@46.250.225.12 "sleep 60 && pm2 logs sales-agent-v4 --nostream --lines 20 2>&1"`

---

## [BugFix] Fix error in: ssh root@46.250.225.12 "sleep 60

**Problem:** [TAILING] Tailing last 20 lines for [sales-agent-v4] process (change the value with --lines option)
/root/.pm2/logs/sales-agent-v4-error.log last 20 lines:
/root/.pm2/logs/sales-agent-v4-out.log last 20 lines:
32|sales-a | Starting Sales Agent v4....
**Fix command:** `ssh root@46.250.225.12 "
TOKEN=\$(grep 'THUYMAC_TG_BOT_TOKEN=' /root/ai-system-v2/.env | cut -d= -f2)
echo 'getWebhookInfo:'
curl -s \"https://api.tel`

---

## [BugFix] Fix ECONNREFUSED

**Problem:** ECONNREFUSED
**Solution:** Tôi đã có đủ data. Đây là kết quả deep research:

---

## Deep Research: Telegram Multi-Bot Polling — Root Cause & Fix

### Root Cause chính xác

Từ tất cả sources, **409 Conflict KHÔNG xảy ra giữa bots khác token**. Lỗi luôn là **cùng 1 token bị poll bởi 2+ clients**. Nguyên nhân cụ thể trong ca...

---

## [BugFix] Fix: Runtime error

**Problem:** # Telegram 409 Conflict Fix Checklist

## Root Cause
The error "409 Conflict: terminated by other getUpdates request" occurs because **the same bot token is being polled simultaneously by multiple clients**. As stated in the guide, "Telegram allow...
**Solution:** Tôi đã có đủ data. Đây là kết quả deep research:

---

## Deep Research: Telegram Multi-Bot Polling — Root Cause & Fix

### Root Cause chính xác

Từ tất cả sources, **409 Conflict KHÔNG xảy ra giữa bots khác token**. Lỗi luôn là **cùng 1 token bị poll bởi 2+ clients**. Nguyên nhân cụ thể trong ca...

---

## [BugFix] Fix exit code 255: scp /tmp/ai-system-v2-deploy.tar.gz root@46.250.225.12:/tmp/

**Problem:** Exit code 255
[PM2] Applying action deleteProcessId on app [sales-agent-v4](ids: [ 32 ])
[PM2] [sales-agent-v4](32) ✓
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──...
**Fix command:** `ssh root@46.250.225.12 "ls /root/ai-system-v2/node_modules/node-telegram-bot-api/index.js 2>/dev/null && echo 'PKG OK' || echo 'PKG MISSING'"`

---

## [Decision] Tôi sẽ dùng user's instruction — **deep research t...

**Decision:** Tôi sẽ dùng user's instruction — **deep research trước khi fix tiếp**. Tạm để bot chạy, lưu bài học:

---

## [Decision] Ảnh gốc có **nền trắng xung quanh** bức tranh. Fol...

**Decision:** Ảnh gốc có **nền trắng xung quanh** bức tranh. Folder `cropped` đã crop sát tranh rồi. Tôi sẽ dùng ảnh từ folder `cropped` (đã crop sẵn), không cần xóa background nữa.

---

## [Decision] Tôi sẽ dùng **Option A cho cả hai**:

**Decision:** Tôi sẽ dùng **Option A cho cả hai**:
**Context:** - **Thủy Mạc**: AI tạo background phòng khách có **khung tranh trống** → Sharp paste tranh gốc vào khung → ảnh final 100% giữ nguyên tranh

---

## [Decision] 2. **Thêm tab "Chiến lược"** trong Marketing — chọ...

**Decision:** 2. **Thêm tab "Chiến lược"** trong Marketing — chọn ảnh AI từ kho → nhập prompt tạo caption → nút "Áp dụng chiến lược" → upload lên fanpage
**Context:** 1. **Bỏ phần hình ảnh trong Marketing** → chuyển lưu ảnh vào tab Sản phẩm

---

## [Decision] │  │ Chọn ảnh AI  │  │ Caption                    ...

**Decision:** │  │ Chọn ảnh AI  │  │ Caption                      │ │
**Context:** │  ┌──────────────┐  ┌──────────────────────────────┐ │ | │  │ từ kho SP    │  │                              │ │

---

## [Learning] - Chỉ cần thêm **chat widget frontend** trên website + ...

**Insight:** - Chỉ cần thêm **chat widget frontend** trên website + **webhook API** nhận tin từ widget

---

## [Learning] Deploy lên VPS — lần này cần upload cả `node_modules` m...

**Insight:** Deploy lên VPS — lần này cần upload cả `node_modules` mới vì thêm package:

---

## [Learning] Thực ra `parseProduct` trong API trả về cả `backgrounds...

**Insight:** Thực ra `parseProduct` trong API trả về cả `backgrounds` lẫn `background` — nên frontend access được cả hai.

---

## [Learning] Type error — Product type dùng `background` không phải ...

**Insight:** Type error — Product type dùng `background` không phải `backgrounds`.

---

## [Learning] Cần fix: dùng `background` trong frontend nhưng gửi API...

**Insight:** Cần fix: dùng `background` trong frontend nhưng gửi API với `backgrounds`.

---

## [Config] SSH config

**Command:** `ssh root@46.250.225.12 "pm2 describe sales-agent-v4 2>&1 | grep -E 'status|pid|uptime|restarts' && echo '---' && ss -tnp | grep 8:443 | grep 'node /ro`
**Config:**
```
│ status            │ online                                   │
│ restarts          │ 0                                        │
│ uptime            │ 86s                                      │
│ pid path          │ /root/.pm2/pids/sales-agent-v4-31.pid    │
│ unstable restarts │ 0                                        │
---
0
active TG connections
```

---

## [Config] Environment variables (.env)

**Command:** `cd "C:/Abuss/ai-system-v2" && npx tsc 2>&1 && tar czf /tmp/ai-system-v2-deploy.tar.gz dist/ package.json package-lock.json ecosystem.config.js .env --`
**Config:**
```
[PM2] Applying action deleteProcessId on app [sales-agent-v4](ids: [ 31 ])
[PM2] [sales-agent-v4](31) ✓
┌────┬─────────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──────────┬──────────┐
│ id │ name                    │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │ cpu      │ mem      │ user     │ watc...
```

---
> Auto-generated by longbrain-stop-hook v2.0 | [[32 Bai Hoc Duc Ket]]
